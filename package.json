{
  "name": "opentdf-mcp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "http-server . -p 8080 -c-1",
    "dev": "http-server . -p 8080 -c-1 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["opentdf", "encryption", "decryption", "mcp", "demo"],
  "author": "",
  "license": "ISC",
  "description": "OpenTDF MCP Gateway with end-to-end encryption/decryption demo",
  "dependencies": {
    "@opentdf/sdk": "^0.4.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
